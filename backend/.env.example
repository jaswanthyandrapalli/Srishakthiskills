PORT=5000
MONGODB_URI=mongodb://localhost:27017/srisakthi
JWT_SECRET=supersecretjwtkeyforsrisakthisarees
JWT_EXPIRE=30d

# Cloudinary (Optional, falls back to local data URLs if not set)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Razorpay (Optional, falls back to mock payments if not set)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# SMTP Email (Required for OTP emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Sri Sakthi Sarees <no-reply@srisakthi.com>"

# Frontend URL
FRONTEND_URL=http://localhost:3000
